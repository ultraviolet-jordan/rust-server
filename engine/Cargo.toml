[package]
name = "engine"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
criterion = "0.5.1"

[dependencies]
dotenv = "0.15.0"
rand = "0.8.5"
cache = { path = "../cache" }
math = { path = "../math" }
rsmod-pathfinder = "1.0.0-SNAPSHOT"

[[bench]]
name = "fib"
harness = false